{
  "name": "parcel-transformer-markdoc",
  "version": "1.0.0",
  "description": "Parcel transformer to load Markdown files when using Markdoc",
  "main": "./lib/index.js",
  "module": "./esm/index.js",
  "scripts": {
    "build": "parcel build src/index.js"
  },
  "author": "Charlie Gerard | @devdevcharlie",
  "license": "MIT",
  "keywords": [
    "parcel",
    "parcel-bundler",
    "parcel-plugin",
    "bundler",
    "plugin",
    "transformer",
    "markdown",
    "markdoc"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/charliegerard/parcel-transformer-markdoc.git"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.3.2",
    "@parcel/core": "^2.3.2"
  },
  "peerDependencies": {
    "parcel": "^2.5.0"
  },
  "dependencies": {
    "@parcel/plugin": "^2.2.0",
    "@markdoc/markdoc": "^0.1.2"
  },
  "engines": {
    "node": ">= 12.0.0",
    "parcel": "^2.2.0"
  }
}
